
using System;
using System.Windows.Forms;

namespace Hamburgueria.UI
{
    static class Program
    {
        // Simulação de um ponto de entrada básico para WinForms
        [STAThread]
        static void Main()
        {
            // Apenas para simular a compilação de um projeto WinForms
            Console.WriteLine("Hello World from Hamburgueria.UI");
            // Application.EnableVisualStyles();
            // Application.SetCompatibleTextRenderingDefault(false);
            // Application.Run(new Form1()); // Comentado para não travar o ambiente sem interface gráfica
        }
    }
}

